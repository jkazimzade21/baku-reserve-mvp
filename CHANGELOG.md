## Unreleased

- feat(prep-notify): pre-arrival food prep with mock payments behind flag (new preorder quote/confirm API, payment provider abstraction, Expo prep flow, docs/tests)
- feat(auth): require Auth0 JWTs on the API and add a native Auth0 login gate in the Expo app
